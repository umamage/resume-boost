services:
  # Backend API Service
  - type: web
    name: resume-boost-backend
    env: python
    region: oregon
    plan: free
    # Remove rootDir, run from repo root
    buildCommand: pip install -r requirements.txt
    # Explicitly set app directory for uvicorn
    startCommand: uvicorn app.main:app --app-dir backend --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.1
      - key: DATABASE_URL
        fromDatabase:
          name: resume-boost-db
          property: connectionString

  # Frontend Service
  - type: web
    name: resume-boost-frontend
    env: node
    region: oregon
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm run preview -- --host 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: VITE_API_URL
        fromService:
          type: web
          name: resume-boost-backend
          property: host

# PostgreSQL Database
databases:
  - name: resume-boost-db
    plan: free
    databaseName: resume_boost
    user: resume_boost_user
