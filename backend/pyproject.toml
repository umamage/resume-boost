[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic[email]",
    "python-multipart",
    "pytest",
    "httpx",
    "sqlalchemy",
    "psycopg2-binary",
]
